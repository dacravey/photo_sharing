<div class="container">

<h1>Photo Sharing!<small> Under construction</small></h1>

<% if current_user %>
  <%= image_tag current_user.avatar.url(:medium) %>
  <% end %>

<ul>
<% User.all.each do |person| %>
  <li><% person.email %> <% person.avatar %></li>
  <% end %>
</ul>
</div>
